# Copy this value into ~/.Renviron or set it for the current R session.
# Never commit a real database password.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require

# Must match the app deployment when monitoring allocation expiry.
ALLOCATION_EXPIRY_MINUTES=60
